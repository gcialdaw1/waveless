// Para iniciar el watcher en scss
npx sass src/scss/main.scss dist/styles.css --watch

Dado a que el CORS no permite el acceso a otros archivos si se abre directamente el HTML, es necesario visualizar la app con un live server o servidor local.